Important Information:

Template JSON File (incomplete) can be found in final_dataset.json
Note that the code has the run pipeline functions in comments and cannot be run until removed.
Furthermore, the code will prompt for API-Key input which can be found in the Google AI Studio.
If you want to remove this feature, kindly add the API-Key as an environment variable.
BTW, the code will require access to your hard disk as it will store live csv results on ur disk in "live_results.csv" which will store raw data of 1 and 0's.
